package ru.sbrf.ufs.platform.audit.model;

/**
 * Заглушка критичности события аудита из платформенной модели Единого аудита.
 * <p>
 * Реальная библиотека подключается зависимостью {@code audit-client-core2}; класс оставлен в соседнем
 * пакете, чтобы модуль собирался без исходников клиента.
 */
public enum CriticalityEnum {
  /** Критичное событие, требующее приоритетной обработки. */
  CRITICAL,
  /** Некритичное событие. */
  UNCRITICAL
}
